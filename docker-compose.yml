services:
  web:
    build: .
    environment:
      - TELEMETRY_DATA_PATH=/app/data/telemetry-events.jsonl
    ports:
      - "127.0.0.1:4173:4173"
    volumes:
      - telemetry_data:/app/data
    restart: unless-stopped

  mail:
    image: stalwartlabs/stalwart:latest
    ports:
      - "25:25"       # SMTP
      - "465:465"     # SMTPS
      - "587:587"     # Submission
      - "993:993"     # IMAPS
      - "8443:443"    # Admin/JMAP web UI (mapped to 8443 to avoid Caddy conflict)
    volumes:
      - stalwart_data:/opt/stalwart
    environment:
      - MAIL_DOMAIN=crucifyai.com
      - ADMIN_SECRET=${MAIL_ADMIN_PASSWORD}
    restart: unless-stopped

  webmail:
    image: roundcube/roundcubemail:latest
    ports:
      - "127.0.0.1:8079:80"
    environment:
      - ROUNDCUBEMAIL_DEFAULT_HOST=ssl://mail
      - ROUNDCUBEMAIL_DEFAULT_PORT=993
      - ROUNDCUBEMAIL_SMTP_SERVER=tls://mail
      - ROUNDCUBEMAIL_SMTP_PORT=587
    depends_on:
      - mail
    restart: unless-stopped

volumes:
  stalwart_data:
  telemetry_data:
